events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        
        location /web/api/metrics {
            proxy_pass https://socialsundae.io/api/metrics;
        }
    }
}
